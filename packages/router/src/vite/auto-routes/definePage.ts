import type { PageOptions } from '../core/types.js'

/**
 * 定义页面配置的函数
 *
 * 此函数在运行时无任何作用，仅用于自动化路由识别页面配置
 *
 * @param _options 页面配置选项，类型为 PageOptions
 */
export function definePage(_options: PageOptions): void {
  return
}

export type { PageOptions }
